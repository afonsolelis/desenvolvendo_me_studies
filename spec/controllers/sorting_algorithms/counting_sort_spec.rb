# frozen_string_literal: true

require 'rails_helper'

RSpec.describe SortingAlgorithms::CountingSortController, type: :controller do
  describe 'Counting sort' do
    # it 'should sort the following array: [-5, -10, 0, -3, 8, 5, -1, 10]' do
    #   params = {
    #     array: [-5, -10, 0, -3, 8, 5, -1, 10]
    #   }
    #   expected_response = { 'message' => [-10, -5, -3, -1, 0, 5, 8, 10] }
    #   post :create, params: params

    #   expect(response).to have_http_status(:ok)
    #   expect(response.parsed_body).to eq(expected_response)
    pending "add some examples (or delete) #{__FILE__}"
  end
end
